/**
 * Ignore this step if yout don´t have a tickets/issues registry assigned for the commits,
 *    or the posibilite to create groups of tickets/issues  
 * The target of this step is to create groups of subtasks related with commits.
 * 
 * Feature-1 -> c1, c2, c3...
 * Feature-2 -> c1000, c1001, ...
 * 
 */